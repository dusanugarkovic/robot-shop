apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  labels:
    app: caller
  name: caller
spec:
  replicas: 1
  strategy: {}
  template:
    metadata:
      labels:
        app: caller
    spec:
      containers:
        - image: gcr.io/peppy-vertex-158106/robot-shop/rs-caller:latest
          name: caller
          env:
            - name: SERVER_HOST
              value: "http://caller"
            - name: SERVER_PORT
              value: "8080"
            - name: WEB_HOST
              value: "http://web"
            - name: WEB_PORT
              value: "8080"
            - name: LOAD_CONCURRENCY
              value: "3"
            - name: LOAD_REQUESTS_PER_SECOND
              value: "3"
          ports:
            - containerPort: 8080
          resources:
            limits:
              cpu: 400m
              memory: 300Mi
            requests:
              cpu: 200m
              memory: 200Mi
      restartPolicy: Always